
# never commit key material
lgalaxy.txt*
id_ed25519*
id_rsa*
*.pem

# 0.3.54: the relay's Galactic Database catalogue — the galaxy's, not the repo's
gdb.json
gdb.json.*

# smoke screenshots
_scratch/
